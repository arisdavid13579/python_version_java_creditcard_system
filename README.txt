This is a python version of the Java credit card database (case study) project found at:

I tried to make it as much of a copy as possible, without much regard to recoding the Java project into a pythonic way. Future versions might be refactored into more python-like code.